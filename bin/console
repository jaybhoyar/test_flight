#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "test_flight"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

payload = {
  app_name: "neeto_chat",
  device_platform: "ios",
  device_token: "w40983u0",
  alert: "Hi",
  body: "Don't miss out on this"
}

client = TestFlight::PushNotification.new(payload).send_notification
puts client

require "irb"
IRB.start(__FILE__)
